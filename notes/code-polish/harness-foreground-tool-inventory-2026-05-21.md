# Harness Foreground Tool Inventory

Date: 2026-05-21
Anchor task: #327
RFC: `notes/rfc/oneesama-harness-cache-tool-stability-rfc-2026-05-21.md`

## Purpose

This inventory pins the foreground tool surface that affects Oneesama cache
locality. A new foreground tool is a migration, not a casual helper. Prefer one
of these before adding a schema:

1. Add a parameter to an existing stable foreground tool.
2. Route the capability through bounded worker delegation.
3. Route bot-owned Browser / Computer Use through `control_shared_browser_surface`.
4. Route existing shared macOS app operation through `control_shared_app_window`.
5. Add a documented approval gate if the operation mutates external state.

## Realtime Foreground Hashes

These hashes are generated by `RealtimeToolSchemaStableHash` and pinned in
`internal/meetingagent/realtime_tool_hash_test.go`.

| Surface | Hash |
|---|---|
| Realtime tools, demo surface disabled | `49468ff77ff6f8c0fd61fd220eedf776f82ac265c066b67a1edceaffb412e563` |
| Realtime tools, demo surface enabled | `7e009a3ce3f379e9d037041e7b943f77f3d848ef4f26ebc21c9cf06df8723c11` |

Changing either hash is allowed only when the same commit also updates this
inventory or a task/RFC note explaining why the foreground schema changed.

## Realtime Tool Classes

Source of truth in code: `internal/meetingagent/realtime_tool_inventory.go`.

| Class | Meaning | Examples |
|---|---|---|
| `stable_foreground` | Always part of the realtime foreground schema. Changes here can invalidate prompt/tool cache and need review. | `delegate_to_worker`, `send_meet_chat`, `share_existing_app_window`, `control_shared_app_window`, `current_user_identity`, `now` |
| `optional_foreground` | Foreground schema exposed only behind a runtime/product gate. | `open_shared_browser_surface`, `create_shared_workspace`, `control_shared_browser_surface`, `stop_shared_browser_surface` |
| `deprecated_alias` | Compatibility shape kept behind non-foreground callers. New prompts and code should use the stable replacement. | `delegate_to_codex`, `delegate_status` |
| `worker_only` | Not exposed to realtime foreground. Must stay behind worker/session capabilities. | GitHub/Linear/Slack/Notion fine-grained lookup, memory read/write, URL fetch, visual avatar/HUD tools, Slack file/canvas fetch/edit helpers, shell/file tools, deep codebase work |

## Current Realtime Inventory

| Tool | Class | Gate |
|---|---|---|
| `delegate_to_worker` | `stable_foreground` | `session_capabilities` |
| `worker_status` | `stable_foreground` | `session_capabilities` |
| `send_meet_chat` | `stable_foreground` | `active_meeting_session` |
| `present_video_stage` | `stable_foreground` | `active_meeting_session` |
| `stop_video_stage` | `stable_foreground` | `active_meeting_session` |
| `list_shareable_windows` | `stable_foreground` | `active_meeting_session` |
| `share_existing_app_window` | `stable_foreground` | `active_meeting_session` |
| `control_shared_app_window` | `stable_foreground` | `active_meeting_session_and_host_computer_use` |
| `open_shared_browser_surface` | `optional_foreground` | `demo_surface_enabled` |
| `create_shared_workspace` | `optional_foreground` | `demo_surface_enabled` |
| `control_shared_browser_surface` | `optional_foreground` | `demo_surface_enabled_and_policy` |
| `stop_shared_browser_surface` | `optional_foreground` | `demo_surface_enabled` |
| `read_meet_chat` | `stable_foreground` | `active_meeting_session` |
| `meet_participants` | `stable_foreground` | `active_meeting_session` |
| `active_speaker` | `stable_foreground` | `active_meeting_session` |
| `current_user_identity` | `stable_foreground` | `workspace_identity_read` |
| `resolve_speaker_identity` | `stable_foreground` | `workspace_identity_read` |
| `calendar_attendees` | `stable_foreground` | `workspace_connector_read` |
| `now` | `stable_foreground` | `dynamic_value_tool` |

## Worker-Only Inventory

Worker-only means "available to bounded workers or session capability sets, not
to the realtime foreground schema."

| Surface | Tools |
|---|---|
| Slack worker bridge | `read_doc`, `memory_search`, `memory_get`, `memory_write`, `person_memory`, `exa_search`, `exa_contents`, `runtime_status`, `heartbeat_log`, `suggest_action` |
| Slack API worker methods | `fetch_thread`, `fetch_channel_history`, `fetch_canvas`, `fetch_image`, `fetch_file`, `create_canvas`, `edit_canvas` |
| Demo-surface worker session | Browser/CU adapter actions behind `open_shared_browser_surface` / `control_shared_browser_surface`; meeting chat and Slack mutation tools are blocked |
| Slack triage worker session | code/search/read helpers for planning only; `send_message` and unsupported interactive tools stay blocked |
| Realtime background job | `delegate_to_worker` carries GitHub/Linear/Slack/Notion/calendar/document/URL lookup and legacy Codex-style work requests |
| Runtime avatar/HUD | Video, speaking, thinking, and tool-call status should be driven by audio/tool/job telemetry, not foreground visual-only model tools |

## Migration Checklist

When changing a foreground tool schema:

1. Update `realtime_tool_inventory.go` with the tool class, gate, and notes.
2. Update `harness-foreground-tool-inventory-2026-05-21.md` or add a dated
   companion note if the change is a migration.
3. Update the golden hashes in `realtime_tool_hash_test.go`.
4. Explain whether the capability could have been a worker skill or
   `control_shared_browser_surface` parameter instead.
5. Run `go test ./internal/meetingagent -run 'TestRealtimeTool' -count=1` and
   the normal full validation suite.

If any of those steps feels excessive, the proposed change is probably too
small for a new foreground tool.
