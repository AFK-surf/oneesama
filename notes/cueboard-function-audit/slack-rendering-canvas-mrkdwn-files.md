# Slack Rendering / Canvas / Mrkdwn / Files Function Parity Audit

Task: task #167

Cueboard root: `/Users/pengx17/Documents/cueboard/agent-framework/deploy/docker/data/slack-agent/runtime/repos/cueboard-main/agent-framework/internal/bridge/slack`

Oneesama comparison root: `/Users/pengx17/.slock/agents/34e14f86-d6a4-48e0-aa7b-9b98507eb9de/worktrees/oneesama-go-rewrite/internal/slackagent`

Scope: Slack markdown/mrkdwn conversion, Block Kit rendering, mention reply rendering, feedback footer rendering, Slack file upload/staging, Slack Canvas create/edit/fetch helpers, post-meeting Canvas publishing, worker-result Canvas publishing, and HTML-to-markdown conversion for Canvas content. Generic Slack tool registration and action routing were covered in task #165; deeper meeting ASR/summary generation is deferred to task #169.

## Summary

| Status           | Function groups | Notes                                                                                                       |
| ---------------- | --------------: | ----------------------------------------------------------------------------------------------------------- |
| identical        |               0 | Packages/types differ (`slackapi.Block` vs JSON maps), so no scoped file is byte-identical.                 |
| verbatim_port    |              39 | Markdown/mrkdwn conversion, Block Kit sectioning, HTML-to-markdown, file path staging, and list cleanup.    |
| partial          |              31 | Reply rendering, file upload, Canvas publishing, meeting artifact rendering, and long-result Canvas paths.  |
| drift            |               7 | Runtime has equivalent helpers but different safety, fallback, or memory side effects.                      |
| missing          |              10 | Canvas fetch/download, generic Canvas tool actions, thinking extraction, and several Cueboard post effects. |
| product_excluded |               4 | Proactive reply footer UI intentionally removed after Peng feedback; direct image/audio generation out.     |
| unreviewed       |               0 | Every scoped Cueboard behavior group is covered below.                                                      |

## High-Level Findings

Oneesama's low-level rendering ports are strong. `mrkdwn.go`, `mrkdwn_blocks.go`, and `html_markdown.go` are near-verbatim ports with cueboard parity tests, and the main assistant/triage/worker result paths now post Block Kit blocks with Slack mrkdwn fallback text.

The Canvas and file layer is more mixed:

- **P0 RESOLVED (5/18, commit `12e09b2`; tightened 5/19):** `slack.uploadFile` now routes through `slackWorkspaceFileResolver.resolveLocalUploadPath`. Slack-triggered uploads are restricted to the workspace dir; only trusted direct temp artifacts such as `oneesama-upload-*` / `cueboard-upload-*` are auto-staged into `.tmp/slack-upload-staging/` before upload. Arbitrary nested `/tmp` files are rejected. Regression tests in `slack_api_tool_upload_safety_test.go`.
- **P1 RESOLVED (5/18, commits `1240484` + `94cd49b`):** `slack_api(fetch_canvas)` and `slack_api(fetch_image)` are now `active`. Canvas fetch downloads HTML via files.info → url_private_download → `htmlToMarkdown` with size cap; image fetch downloads bytes and optionally returns inline base64 plus a mime data URL ready for multimodal models. `create_canvas` / `edit_canvas` remain `registered_unavailable` pending a product decision on whether assistant-visible Canvas authoring belongs in `slack_api` or stays internal to meeting/worker publication.
- **P1 RESOLVED (5/18, commit `a353b79`):** `CanvasPublisher.publishSlackCanvas` now performs a sanitize-and-retry pass when Slack returns a validation-class error (`invalid_markdown` / `markdown_too_long` / `parse_error` etc.). `sanitizeMarkdownForSlackCanvas` strips raw HTML, flattens GFM tables to bullets, drops code-fence language hints, removes footnotes, collapses excessive blank lines. Successful retries are tagged `sanitized_markdown_retry_after:<original_error>` in `result.Detail` for audit visibility.
- P1: reply post memory side effects are uneven. Cueboard's `postSlackThreadReply` always records thread-ledger outbound after Slack post; Oneesama records direct triage replies and meeting delivery separately, but generic `Poster.PostMessage`, worker result posts, and event dispatch posts do not uniformly write the thread ledger. **Still open** — Phase 5 P1 centralized post ledger work remains.

## Function Inventory

| Source file                            | Function(s)                                                                                                                                                                        | Lines           | Status           | Oneesama target                                                                                | Evidence                                                                    | Notes                                                                                                                                                                                          |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mrkdwn.go`                            | `SetLinearWorkspaceSlug`, `SetLinearIssuePrefixes`, `markdownToMrkdwn`, `markdownToSlackFallbackText`, `markdownishToMrkdwn`                                                       | 54-128          | verbatim_port    | `mrkdwn.go`                                                                                    | `cueboard_mrkdwn_parity_test.go`                                            | Linear issue linkification, Markdown link/header/list/table conversion, and mrkdwn-preserving mode are ported.                                                                                 |
| `mrkdwn.go`                            | `sanitizeSlackCodeSegment`, `breakSlackAutolink`, `splitCodeSegments`, `transformText`, `transformTextPreservingMrkdwn`, `convertCommonMarkdown`                                   | 130-228         | verbatim_port    | `mrkdwn.go`                                                                                    | `cueboard_mrkdwn_parity_test.go`                                            | Inline-code URL autolink breaking and code-segment preservation are ported.                                                                                                                    |
| `mrkdwn.go`                            | `linkifyLinearIssues`, `convertEmphasis`, `convertStrongEmphasis`, `convertMarkdownTable`, `parseTableRow`                                                                         | 232-339         | verbatim_port    | `mrkdwn.go`                                                                                    | `cueboard_mrkdwn_parity_test.go`                                            | Markdown tables become readable bullet-like text; only configured Linear prefixes are linked.                                                                                                  |
| `mrkdwn_blocks.go`                     | `markdownToBlocks`, `splitMarkdownSections`, `allSameChar`, `leadingHeaderHashes`                                                                                                  | 12-131          | verbatim_port    | `mrkdwn_blocks.go`                                                                             | `cueboard_mrkdwn_blocks_parity_test.go`                                     | Oneesama emits JSON map blocks instead of `slackapi.Block`, but behavior matches Cueboard section/header/divider splitting.                                                                    |
| `mention_render.go`                    | `splitMrkdwnChunks`                                                                                                                                                                | 116-137         | verbatim_port    | `mrkdwn_blocks.go:splitMrkdwnChunks`                                                           | `cueboard_mrkdwn_blocks_parity_test.go`                                     | Moved from mention rendering into the block renderer; still splits at newlines before hard cutting.                                                                                            |
| `html_markdown.go`                     | `htmlToMarkdown`, `replaceTag`, `convertLinks`, `extractAnchorHref`, `stripAllTags`                                                                                                | 24-163          | verbatim_port    | `html_markdown.go`                                                                             | `cueboard_html_markdown_parity_test.go`                                     | HTML-to-Markdown behavior is ported, but currently has no live Canvas fetch caller.                                                                                                            |
| `mention_render.go`                    | `buildSlackThreadReplyBlocks`, `softenSlackThreadReplyMarkdown`                                                                                                                    | 38-65 / 222-236 | partial          | `mention_render.go`                                                                            | `cueboard_mention_parity_test.go`; `cueboard_bridge_thread_surface_test.go` | Content block rendering and italic-softening are ported. Thinking truncation uses `...` instead of `…`; footer behavior intentionally differs.                                                 |
| `mention_render.go`                    | `postThreadReply`, `postThreadReplyInterim`, `postSlackThreadReply`                                                                                                                | 24-80           | partial          | `Service.PostMessage`, `executeSlackTriageDirectActions`, `postSlackWorkerResult`, event posts | `handler_inbound_test.go`; `cueboard_bridge_thread_surface_test.go`         | Oneesama posts through a shared `Poster` with dedupe and blocks. It does not provide one central helper that always records thread-ledger outbound.                                            |
| `mention_render.go`                    | `buildReplyFooter`, `withSessionID`                                                                                                                                                | 82-114          | product_excluded | `buildReplyFooterBlocks`                                                                       | Peng 2026-05-17 feedback; `cueboard_mention_parity_test.go`                 | Proactive feedback footer was removed as annoying. Emoji feedback and explicit corrections remain the product path.                                                                            |
| `mention_render.go`                    | `addReaction`, `removeReaction`                                                                                                                                                    | 139-157         | partial          | `finishMentionReaction`, `actionGenericSlackForm(reactions.add)`                               | `service_events.go`; `slack_api_tool_messages.go`                           | Reaction helpers exist via event/Slack API paths, not as Bridge methods with Slack-go error string handling.                                                                                   |
| `mention_render.go`                    | `stripMention`, `latestAssistantTextSince`, `historyMessageText`                                                                                                                   | 159-194         | partial          | `stripSlackUserMention`, `latestAssistantTextSince`, `assistant_history.go:historyMessageText` | `cueboard_mention_parity_test.go`                                           | Mention stripping/history text extraction are ported under Oneesama event/history types.                                                                                                       |
| `mention_render.go`                    | `extractThinking`                                                                                                                                                                  | 196-220         | missing          | none in Slack mention render path                                                              | source audit                                                                | Oneesama has post-meeting `<think>` stripping, but Slack mention/worker result rendering does not split model reasoning into thinking/context blocks.                                          |
| `file_upload.go`                       | `resolveLocalUploadPath`, `ensurePathWithinWorkspace`, `stageTempUploadPath`, `isAutoStagingTempPath`, `pathWithinRoot`, `copyFileForSlackUpload`                                  | 78-244          | verbatim_port    | `file_upload.go`                                                                               | `cueboard_file_upload_parity_test.go`                                       | Workspace-bound path checking and trusted temp artifact staging are ported as `slackWorkspaceFileResolver`.                                                                                    |
| `file_upload.go`                       | `uploadLocalFile`                                                                                                                                                                  | 21-76           | drift            | `UploadSlackFile`, `slackAPITool.actionUploadFile`                                             | `slack_file_upload_test.go`; source audit                                   | Raw Slack external upload works, but the gateway currently bypasses the workspace resolver and does not add Cueboard's ledger/reservation side effects.                                        |
| `slack_file_upload.go`                 | `UploadSlackFile`, `slackGetUploadURLExternal`, `slackUploadToExternalURL`, `slackCompleteUploadExternal`, `slackFileInfoPermalink`, `callSlackFormAPI`                            | n/a             | partial          | Oneesama-only raw API layer                                                                    | `slack_file_upload_test.go`; `handler_meeting_webhook_test.go`              | Oneesama replaced Slack-go with direct API calls and testable injected endpoint/client. This is fine for meeting artifacts, unsafe when tool path unguarded.                                   |
| `slack_api_tool_canvas.go`             | `actionCreateCanvas`, `actionEditCanvas`, `editCanvas`, `decodeCanvasEditResponse`                                                                                                 | 17-38 / 64-172  | partial          | `CreateSlackCanvas`, `EditSlackCanvas`, `CanvasPublisher.publishSlackCanvas`                   | `canvas_publisher_test.go`; `handler_meeting_webhook_test.go`               | Native Canvas create/edit exists for publisher flows, but generic `slack_api` Canvas actions still return unavailable.                                                                         |
| `slack_api_tool_canvas.go`             | `actionFetchCanvas`, `downloadCanvasContent`, `downloadCanvasFromURL`                                                                                                              | 40-62 / 89-111  | missing          | none                                                                                           | `slack_api_tool_messages.go` matrix; `html_markdown.go` unused caller       | Cueboard can fetch Canvas body and convert Slack HTML to Markdown. Oneesama has converter only, no fetch/download path.                                                                        |
| `meet_publish.go`                      | `postMeetingSummaryToSlack`, `meetingSummaryTargetChannel`, `createCanvas`                                                                                                         | 19-179          | partial          | `HandleMeetingWebhook`, `publishMeetingSummary`, `CanvasPublisher`, `CreateSlackCanvas`        | `handler_meeting_webhook_test.go`; `canvas_publisher_test.go`               | Result reservation/dedupe and native Canvas publish are covered. Oneesama skips Cueboard's pilot-DM fallback when no Slack ref is known.                                                       |
| `meet_publish.go`                      | `buildCanvasMarkdown`, `appendCanvasBulletSection`, `appendCanvasActionItemSection`                                                                                                | 183-267         | partial          | `buildMeetingCanvasMarkdown`, `meetingCanvasArtifact`, `renderCanvasMarkdown`                  | `meeting_webhook_render_test.go`; `cueboard_meet_publish_parity_test.go`    | Cueboard meeting sections are ported and expanded with transcript/audio artifact links. Generic `CanvasPublisher` file mode has a lighter fallback shape.                                      |
| `meet_publish.go`                      | `sanitizeCanvasMarkdown`, `stripToPlainMarkdown`, `normalizeCanvasListLine`, `canvasNumberedListPrefix`                                                                            | 272-385         | partial          | `normalizeCanvasListLine`, `canvasNumberedListPrefix`, structured renderer helpers             | `cueboard_meet_publish_parity_test.go`; source audit                        | List normalization is ported. Full sanitize + plain-markdown retry on `canvases.create` failure is missing.                                                                                    |
| `meeting_webhook_render.go` (Oneesama) | `buildMeetingResultNotification`, `buildMeetingCanvasMarkdown`, `meetingCanvasAttachmentLinks`, `meetingTranscriptText`, `transcriptTextFromArtifact`, `truncateMeetingTranscript` | n/a             | partial          | Oneesama-only meeting artifact renderer                                                        | `meeting_webhook_render_test.go`; `handler_meeting_webhook_test.go`         | Oneesama adds useful artifact/transcript/audio handling beyond Cueboard's original publisher, but it belongs to meeting webhook delivery rather than generic Canvas tooling.                   |
| `canvas_publisher.go` (Oneesama)       | `NewCanvasPublisher`, `Publish`, `ListPublished`, `publishSlackCanvas`, `normalizeCanvasProvider`                                                                                  | n/a             | partial          | Oneesama-only publisher                                                                        | `canvas_publisher_test.go`; `handler_test.go`                               | Strong replacement for meeting/post-worker publication: file manifests, Slack thread fallback, native Canvas, notifications, and dedupe keys.                                                  |
| `service_worker_jobs.go` (Oneesama)    | `postSlackWorkerResult`, `shouldPublishWorkerResultAsCanvas`, `looksLikeLongFormMarkdown`, `workerResultCanvasInput`, `workerResultCanvasTitle`, `workerResultCanvasNotification`  | n/a             | partial          | Oneesama-only long-result publisher                                                            | `cueboard_bridge_thread_surface_test.go`                                    | Long Markdown worker results can become Slack Canvas, especially when a thread contains Canvas files. No equivalent generic Cueboard function, but it closes part of the Canvas authoring gap. |

## P0 Gaps

All P0 gaps in this audit slice are now closed.

- ~~`slack_api_tool_messages.go:208-225` should call the ported `slackWorkspaceFileResolver.resolveLocalUploadPath` before `UploadSlackFile`.~~ **RESOLVED** (commit `12e09b2`): Slack uploads now route through the workspace resolver with `/tmp` auto-staging.

## P1 Gaps

- ~~`slack_api_tool_messages.go:67` and `:390-393` still mark `fetch_canvas`, `create_canvas`, and `edit_canvas` as registered-unavailable~~ **PARTIALLY RESOLVED** (commits `1240484` + `94cd49b`): `fetch_canvas` and `fetch_image` are now `active`. `create_canvas` / `edit_canvas` remain `registered_unavailable` pending product decision on whether assistant-visible Canvas authoring belongs in `slack_api`.
- ~~Cueboard `slack_api_tool_canvas.go:40-111` Canvas fetch is missing.~~ **RESOLVED** (commit `1240484`): `actionFetchCanvas` ports the Canvas download + htmlToMarkdown path with 8 KiB markdown / 512 KiB HTML safety caps.
- ~~`CanvasPublisher.publishSlackCanvas` should add Cueboard's sanitize/plain-markdown retry path.~~ **RESOLVED** (commit `a353b79`): validation-class errors now trigger a single sanitize+retry; the fallback is tagged in `result.Detail`.
- Slack post memory side effects should be centralized. Cueboard `postSlackThreadReply` always recorded outbound into the thread ledger; Oneesama currently records some direct triage replies and meeting deliveries, but worker result posts, app mention event posts, and generic tool posts can bypass durable thread memory. **STILL OPEN** — Phase 5 P1 centralized post ledger work.
- Cueboard posts an action-item follow-up hint after meeting summaries. Oneesama publishes Canvas and projects team memory, but there is no equivalent user-facing "review/create follow-up issues" hint; this may be intentionally replaced by task #166 heartbeat followups, but it should be a product decision.

## Product Exclusions / Intentional Drift

- The proactive reply footer / overflow feedback menu is intentionally removed. Peng explicitly called the footer annoying and asked to learn from emoji keys instead; `buildReplyFooterBlocks` returning nil is correct.
- Direct `image_generation` and `audio_generation` tools are excluded from Oneesama's current product scope. This does not excuse the missing `slack.fetchImage` reader from task #165, which is a Slack file/context helper rather than generation.
- Cueboard's pilot-DM fallback for meeting summaries is not ported. Oneesama skips summary delivery when no Slack ref is known; this is quieter and safer, but needs product confirmation if owner-DM fallback was still desired.

## Open Questions

- Should generic `slack_api` Canvas actions share the `CanvasPublisher` implementation, or should Canvas publishing stay as a dedicated meeting/worker artifact path while `slack_api` remains read/write-message focused?
- Should Canvas fetch be implemented first for mention context (`canvasFiles` body text) or for the explicit `slack.fetchCanvas` tool? The same download/HTML conversion code can serve both.
- Should `Poster.PostMessage` accept an optional ledger recorder or should callers remain responsible for memory side effects? Centralizing it would close several Cueboard parity gaps, but care is needed to avoid recording noisy status/ephemeral-like posts.
- Should `slack.uploadFile` be demoted to unavailable until the workspace resolver is wired, or fixed in place now as a small safety patch?
